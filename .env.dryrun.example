# Phase 10.2.1 PostgreSQL dry-run configuration.
# Copy this file to .env.dryrun on your local machine and replace the
# placeholder values. Never commit .env.dryrun because it may contain secrets.

PHASE10_DRY_RUN_DATABASE_URL=postgresql://dryrun_user:change-me@localhost:5432/mecprecision_dryrun
