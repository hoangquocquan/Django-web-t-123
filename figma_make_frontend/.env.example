# Phase 6A local integration. Values are non-secret and loopback-only.
VITE_DJANGO_ORIGIN=http://127.0.0.1:8000
PHASE6_VITE_PORT=8443

# Browser API code intentionally keeps same-origin, root-relative /api paths.
# Do not set VITE_API_BASE_URL, VITE_CANONICAL_API_BASE_URL, or
# VITE_FOUNDATION_AUTH_BASE_URL for the Phase 6A local integration contract.
