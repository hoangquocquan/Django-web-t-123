"""Django app configuration for the business operator UI."""

from django.apps import AppConfig


class BusinessUiConfig(AppConfig):
    """Dang ky app giao dien business voi Django."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.business_ui"

