[pytest]
DJANGO_SETTINGS_MODULE = config.settings.test
python_files = tests.py test_*.py *_tests.py
testpaths = tests apps
markers =
    legacy_artifact: requires an Owner-provided legacy SQLite artifact and explicit legacy mode
