# PostgreSQL Lab Notes

The `postgres` service simulates Amazon RDS PostgreSQL.

The `backups/` folder is mounted into the container so local backup commands can
write example files without touching production.
