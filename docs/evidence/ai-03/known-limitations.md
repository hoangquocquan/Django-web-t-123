# Known Limitations

- The local model can still produce language that requires human interpretation; validators cover schema, sources, score ownership, prohibited actions and common contradictory pipeline claims.
- JSON vector storage remains the explicit development fallback from AI-01; a production vector database is outside AI-03.
- Email is generated as text only. No email sending tool is connected.
- Real-data validation is local and does not authorize staging or production deployment.
